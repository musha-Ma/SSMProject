ch02-di-xml：基于xml的di给java对象属性赋值

1、di：依赖注入，表示创建对象，给对象属性赋值
2、di实现的两种方式：
    a、xml方式
    b、注解
3、xml方式的di语法
    a、set注入（设置注入），使用类的set方法给属性赋值（常用）
    b、构造注入，使用类的构造方法给属性赋值
4、set注入常见问题：
    a、spring容器只是调用对象的set方法，至于set方法内部spring是不管的，这个由用户自定义
    b、使用set注入要求对象必须要有set方法，否则报错

